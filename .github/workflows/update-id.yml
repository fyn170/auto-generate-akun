name: Update ID 🇮🇩

on:
  schedule:
    - cron: '0 20 * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install PyYAML library
        run: python -m pip install pyyaml

      - name: Proxy Providers
        working-directory: ./proxy_provider
        run: |
          echo "Downloading akun list..."
          python -c "import requests; req = requests.get('https://raw.githubusercontent.com/mahdibland/V2RayAggregator/master/sub/sub_merge_yaml.yml'); open('akun.txt', 'w').write(req.text)"
          sed -i -e '/🇮🇩/!d' akun.txt

          echo "proxies:" > filtered_proxies.json

          python - <<EOF
          import yaml

          with open('akun.txt') as f:
              proxies = f.read()

          proxy_data = yaml.safe_load(proxies)

          filtered_proxies = []

          for proxy in proxy_data:
              proxy_type = proxy.get('type')

              # Check if the proxy is vmess with websocket
              if proxy_type == 'vmess' and proxy.get('network') == 'ws':
                  filtered_proxy = {
                      'name': proxy.get('name'),
                      'server': proxy.get('server'),
                      'port': proxy.get('port'),
                      'type': 'vmess',
                      'network': 'ws',
                      'ws-opts': {
                          'path': proxy.get('ws-opts', {}).get('path'),
                          'headers': {
                              'Host': proxy.get('ws-opts', {}).get('headers', {}).get('Host')
                          }
                      }
                  }
                  filtered_proxies.append(filtered_proxy)

              # Check if the proxy is trojan with websocket
              if proxy_type == 'trojan':
                  filtered_proxy = {
                      'name': proxy.get('name'),
                      'server': proxy.get('server'),
                      'port': proxy.get('port'),
                      'type': 'trojan',
                      'password': proxy.get('password'),
                      'skip-cert-verify': proxy.get('skip-cert-verify')
                  }
                  filtered_proxies.append(filtered_proxy)

          with open('filtered_proxies.json', 'w') as f:
              yaml.dump(filtered_proxies, f, sort_keys=False)

          mv akun.txt free-akun-id.yaml
          sed -i '1i proxies:' free-akun-id.yaml

          cat filtered_proxies.json >> free-akun-id.yaml

      - name: Trigger Bot Update
        uses: peter-evans/repository-dispatch@v1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          repository: fyn170/auto-generate-akun
          event-type: auto-generate-akun

      - uses: stefanzweifel/git-auto-commit-action@v4
        with:
          commit_message: Auto Update
